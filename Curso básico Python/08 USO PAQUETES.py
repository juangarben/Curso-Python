from paquete_modulos.calculos_generales import * #importamos módulo calculos_generales que está en la carpea paquete_modulos
                                                 #así lo podemos usar desde cualquier carpeta
                                                 #para ello la carpeta paquete_modulos debe contener un archivo vacío con el nombre __init__.py
                                                 #podemos importar sólo métodos o clases o con * lo importamos todo aunque esto es más ineficiente porque reserva mucho espacio de memoria
                                                 #También se pueden crear subpaquetes (una carpeta dentro de paquete_modulos) y cada una debe de llevar un archivo vacío con el nombre __init__.py

potencia(4,6)
redondear(5.8)